/**
@module editor/plugin/flash
*/

/**
编辑器的拖放上传插件
@class Flash
@namespace EditorPlugin
@constructor
@param config {Object} 配置对象，详见其Attribute
@example
```
KISSY.use('editor',function(S,Editor){
    S.use('editor/plugin/flash',function(S,Flash){
        // use
    });
});
```
*/

/**
可选。插入 Flash 的默认宽度.
@attribute defaultWidth {Number}
@optional
*/

/**
可选。插入 Flash 的默认高度.
@attribute defaultHeight {Number}
@optional
*/